[package]
name = "minimal-api"
version = "0.1.0"
edition = "2021"
rust-version = "1.88"
publish = false

# Independent consumer workspace: no inherited package/dependency settings.
[workspace]

[dependencies]
berserk = { path = "../../crates/framework" }
