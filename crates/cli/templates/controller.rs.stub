use berserk::{Json, Response, Result};

pub struct {{name}};
impl {{name}} {
    pub fn index() -> Result<Response> { Response::json(&Json::Array(vec![])) }
    pub fn show(id: u64) -> Result<Response> { Response::json(&Json::Object([("id".into(), id.into())].into())) }
}
